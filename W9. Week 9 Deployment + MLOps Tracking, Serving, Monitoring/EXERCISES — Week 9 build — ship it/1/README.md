1. Wrap churn_v2.pkl in the FastAPI app; test with curl including one malformed request — confirm Pydantic rejects it with a clear error.

## to run : 
paste "py -m uvicorn app:app --reload" in terminal
